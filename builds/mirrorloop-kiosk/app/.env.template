# Mirror Loop telemetry env — the bake writes the real /opt/mirror-loop/.env
# into the image (anon key + email filled, password left BLANK on purpose).
# This template is committed; it carries NO secrets.
#
# After first boot, SSH in and set the password:
#   ssh peta@mirrorloop-kiosk.local
#   sudo sed -i 's/^MIRROR_PASSWORD=.*/MIRROR_PASSWORD=YOUR_PASSWORD/' /opt/mirror-loop/.env
#   sudo systemctl restart getty@tty1   # or reboot
# Until then the unit runs DISPLAY-ONLY (camera + effects on the TV) and does
# not post to sinsera.co/mirrorloop.

SUPABASE_URL=https://lkhtgkmivqwgnvzmjbhr.supabase.co
SUPABASE_ANON_KEY=__ANON_KEY__
MIRROR_EMAIL=peta.stockdale@outlook.com
MIRROR_PASSWORD=
UNIT_SLUG=test-zero2w
UNIT_LABEL="TEST ZERO 2 W"
CAMERA_INDEX=0
HEARTBEAT_S=30
